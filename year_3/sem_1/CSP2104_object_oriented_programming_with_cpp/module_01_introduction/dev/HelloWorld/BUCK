# Generated by Buckaroo - https://buckaroo.pm
include_defs('//BUCKAROO_DEPS')

cxx_library(
  name = 'HelloWorld',
  header_namespace = 'HelloWorld',
  srcs = glob([
    'HelloWorld/src/**/*.cpp',
  ]),
  headers = subdir_glob([ # private include files
    ('HelloWorld/detail', '**/*.h'), # they are only accesible inside the library
    ('HelloWorld/detail', '**/*.hpp'),
  ]),
  exported_headers = subdir_glob([ # public include files
    ('HelloWorld/include', '**/*.h'), # those will be exported
    ('HelloWorld/include', '**/*.hpp'), # and accessible via <HelloWorld/header.h>
  ]),
  deps = BUCKAROO_DEPS,
  visibility = ['PUBLIC']
)

cxx_binary(
  name = 'main',
  srcs = ['HelloWorld/apps/main.cpp'],
  deps = [':HelloWorld'],
  visibility = ['PUBLIC']
)

cxx_test(
  name = 'HelloWorldTest',
  srcs = ['HelloWorld/test/HelloWorldTest.cpp'],
  deps = [':HelloWorld'],
  visibility = ['PUBLIC']
)