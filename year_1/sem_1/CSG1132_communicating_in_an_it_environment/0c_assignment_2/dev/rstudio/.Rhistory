lines(
xfit,
yfit,
col = "darkblue"
);
```{r}
h <- hist(
fbDataset.rmOut$Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 20,
# change the limit for x-axis, ylim for y-axis
xlim = c(0,60)
);
xfit <- seq(
min(fbDataset.rmOut$Close_friends),
max(fbDataset.rmOut$Close_friends),
length = 40
);
yfit <- dnorm(
xfit,
mean = mean(fbDataset.rmOut$Close_friends), ,
sd = sd(fbDataset.rmOut$Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(fbDataset.rmOut$Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
fbDataset.rmOut$Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0,60)
);
xfit <- seq(
min(fbDataset.rmOut$Close_friends),
max(fbDataset.rmOut$Close_friends),
length = 40
);
yfit <- dnorm(
xfit,
mean = mean(fbDataset.rmOut$Close_friends), ,
sd = sd(fbDataset.rmOut$Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(fbDataset.rmOut$Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
fbDataset.rmOut$Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0, 60),
ylim = c(0, 20)
);
xfit <- seq(
min(fbDataset.rmOut$Close_friends),
max(fbDataset.rmOut$Close_friends),
length = 40
);
yfit <- dnorm(
xfit,
mean = mean(fbDataset.rmOut$Close_friends), ,
sd = sd(fbDataset.rmOut$Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(fbDataset.rmOut$Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
fbDataset.final <- fbDataset.rmOut
attach(fbDataset.final)
attach(fbDataset.final)
View(fbDataset.final)
summary();
summary(fbDataset.final);
mean(Gender);
```{r}
density <- density(Close_friends);
plot(
density,
main = "CSG1132 A2 Dataset\nKernal Density: Close Friends",
xlab = "Close Friends",
ylab = "Density"
#col = "red"
)
polygon(density, col = "red", border = "red")
```
```{r}
density <- density(Close_friends);
plot(
density,
main = "CSG1132 A2 Dataset\nKernal Density: Close Friends",
xlab = "Close Friends",
ylab = "Density"
#col = "red"
)
polygon(density, col = "red", border = "blue")
```
```{r}
density <- density(Close_friends);
plot(
density,
main = "CSG1132 A2 Dataset\nKernal Density: Close Friends",
xlab = "Close Friends",
ylab = "Density",
col = "red"
);
```
```{r}
h <- hist(
Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0, 60),
ylim = c(0, 20),
width = 5,
height = 5
);
xfit <- seq(
min(Close_friends),
max(Close_friends),
length = 40
);
yfit <- dnorm(
xfit,
mean = mean(Close_friends), ,
sd = sd(Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0, 60),
ylim = c(0, 20),
);
xfit <- seq(
min(Close_friends),
max(Close_friends),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Close_friends), ,
sd = sd(Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0, 60),
ylim = c(0, 20),
);
xfit <- seq(
min(Close_friends),
max(Close_friends),
length = 1
);
yfit <- dnorm(
xfit,
mean = mean(Close_friends), ,
sd = sd(Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
``{r}
h <- hist(
Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0, 60),
ylim = c(0, 20),
);
xfit <- seq(
min(Close_friends),
max(Close_friends),
length = 10
);
yfit <- dnorm(
xfit,
mean = mean(Close_friends), ,
sd = sd(Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Close_friends,
main = "CSG1132 A2 Dataset \n Distribution of Close Friends",
ylab = "Frequency of Close Friends",
xlab = "Close Friends",
col = "grey",
breaks = 5,
# change the limit for x-axis, ylim for y-axis
xlim = c(0, 60),
ylim = c(0, 20),
);
xfit <- seq(
min(Close_friends),
max(Close_friends),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Close_friends), ,
sd = sd(Close_friends)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Close_friends);
lines(
xfit,
yfit,
col = "darkblue"
);
```
wilcox.test(Gender ~ FB_Friends);
wilcox.test(Gender ~ FB_friends);
wilcox.test(FB_friends ~ Gender);
wilcox.test(Gender, FB_friends);
wilcox.test(FB_friends ~ Gender);
kruskal.test(FB_friends ~ Gender);
kruskal.test(Gender ~ FB_friends);
wilcox.test(Gender, FB_friends, paired = TRUE);
wilcox.test(FB_friends ~ Gender);
wilcox.test(Close_friends ~ Gender);
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
breaks = 5,
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = "30"
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = "30"
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = 30
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = c(0, 30)
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
# Compare MPG distributions for cars with
# 4,6, or 8 cylinders
library(sm)
# create value labels
gender.f <- factor(Gender, levels= c(0,1),
labels = c("Female", "Male")
# plot densities
sm.density.compare(FB_friends, Gender, xlab="FB Friends")
title(main="Facebook Friends")
```
```{r}
# Compare MPG distributions for cars with
# 4,6, or 8 cylinders
library(sm)
# create value labels
gender.f <- factor(Gender, levels= c(0,1),
labels = c("Female", "Male")
# plot densities
sm.density.compare(FB_friends, Gender, xlab="FB Friends")
title(main="Facebook Friends")
```
cor.test(Gender, FB_friends, method = "spearman");
cor.test(FB_friends, Gender, method = "spearman");
