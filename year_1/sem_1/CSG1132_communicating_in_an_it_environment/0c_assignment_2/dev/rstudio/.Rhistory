col = "darkblue"
);
```
wilcox.test(Gender ~ FB_Friends);
wilcox.test(Gender ~ FB_friends);
wilcox.test(FB_friends ~ Gender);
wilcox.test(Gender, FB_friends);
wilcox.test(FB_friends ~ Gender);
kruskal.test(FB_friends ~ Gender);
kruskal.test(Gender ~ FB_friends);
wilcox.test(Gender, FB_friends, paired = TRUE);
wilcox.test(FB_friends ~ Gender);
wilcox.test(Close_friends ~ Gender);
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
breaks = 5,
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = "30"
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = "30"
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = 30
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
h <- hist(
Sociability,
main = "CSG1132 A2 Dataset \n Distribution of Sociability",
ylab = "Frequency of Sociability",
xlab = "Sociability",
col = "grey",
ylim = c(0, 30)
);
xfit <- seq(
min(Sociability),
max(Sociability),
length = 100
);
yfit <- dnorm(
xfit,
mean = mean(Sociability), ,
sd = sd(Sociability)
);
yfit <- yfit * diff(h$mids[1:2]) * length(Sociability);
lines(
xfit,
yfit,
col = "darkblue"
);
```
```{r}
# Compare MPG distributions for cars with
# 4,6, or 8 cylinders
library(sm)
# create value labels
gender.f <- factor(Gender, levels= c(0,1),
labels = c("Female", "Male")
# plot densities
sm.density.compare(FB_friends, Gender, xlab="FB Friends")
title(main="Facebook Friends")
```
```{r}
# Compare MPG distributions for cars with
# 4,6, or 8 cylinders
library(sm)
# create value labels
gender.f <- factor(Gender, levels= c(0,1),
labels = c("Female", "Male")
# plot densities
sm.density.compare(FB_friends, Gender, xlab="FB Friends")
title(main="Facebook Friends")
```
cor.test(Gender, FB_friends, method = "spearman");
cor.test(FB_friends, Gender, method = "spearman");
```{r}
library(sm);
male_logins <- subset(FB_Logins, Gender == 1);
female_logins <- subset (FB_Logins, Gender == 0);
logins.f <- factor(
fbDataset.final, levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
FB_Logins,
Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
install.packages("sm")
```{r}
library(sm);
male_logins <- subset(FB_Logins, Gender == 1);
female_logins <- subset (FB_Logins, Gender == 0);
logins.f <- factor(
fbDataset.final, levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
FB_Logins,
Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
library(sm);
male_logins <- subset(FB_Logins, Gender == 1);
female_logins <- subset (FB_Logins, Gender == 0);
logins.f <- factor(
fbDataset.final, levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
FB_Logins,
Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```{r}
library(sm);
male_logins <- subset(FB_Logins, Gender == 1);
female_logins <- subset (FB_Logins, Gender == 0);
logins.f <- factor(
fbDataset.final, levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
FB_Logins,
Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
plot.new()
```
```{r}
library(sm);
plot.new();
male_logins <- subset(FB_Logins, Gender == 1);
female_logins <- subset (FB_Logins, Gender == 0);
logins.f <- factor(
fbDataset.final, levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
FB_Logins,
Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
```{r}
library(sm);
plot.new();
male_logins <- subset(fbDataset.final$FB_Logins, fbDataset.final$Gender == 1);
female_logins <- subset (fbDataset.final$FB_Logins, fbDataset.final$Gender == 0);
logins.f <- factor(
fbDataset.final,
levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
fbDataset.final$FB_Logins,
fbDataset.final$Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
#plot.new();
```{r}
library(sm);
#plot.new();
male_logins <- subset(fbDataset.final$FB_Logins, fbDataset.final$Gender == 1);
female_logins <- subset (fbDataset.final$FB_Logins, fbDataset.final$Gender == 0);
logins.f <- factor(
fbDataset.final,
levels = c(0, 1),
labels = c("Women", "Men")
);
# the comparison plot
sm.density.compare(
fbDataset.final$FB_Logins,
fbDataset.final$Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
```{r}
library(sm);
#plot.new();
male_logins <- subset(fbDataset.final$FB_Logins, fbDataset.final$Gender == 1);
female_logins <- subset (fbDataset.final$FB_Logins, fbDataset.final$Gender == 0);
logins.f <- factor(
fbDataset.final,
levels = c(1, 0),
labels = c("Men", "Women")
);
# the comparison plot
sm.density.compare(
fbDataset.final$FB_Logins,
fbDataset.final$Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
```{r}
library(sm);
#plot.new();
male_logins <- subset(fbDataset.final$FB_Logins, fbDataset.final$Gender == 1);
female_logins <- subset (fbDataset.final$FB_Logins, fbDataset.final$Gender == 0);
logins.f <- factor(
fbDataset.final,
levels = c(1, 0),
labels = c("Men", "Women")
);
# the comparison plot
sm.density.compare(
fbDataset.final$FB_Logins,
fbDataset.final$Gender,
xlab = "Male vs. Female FB Logins",
main = "FB Login distribution by gender"
);
# legend
colfill <- c(
2:(2 + length (levels(logins.f)))
);
legend(
235,
0.014,
legend = c(
"Females",
"Males",
fill = colfill
)
);
```
logins.f
